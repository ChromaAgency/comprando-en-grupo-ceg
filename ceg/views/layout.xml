<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="external_layout_wave_inherit_comprandoengrupoceg" model="ir.ui.view">
        <field name="name">Wave Layout</field>
        <field name="inherit_id" ref="web.external_layout_wave"/>
        <field name="arch" type="xml">
            <li t-if="not forced_vat">
                <t t-esc="company.country_id.vat_label or 'Tax ID'">Tax ID</t>:
                <span t-esc="company.vat">US12345671</span>
            </li>
        </field>
    </record>

</odoo>
